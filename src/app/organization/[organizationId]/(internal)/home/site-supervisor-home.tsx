// This is a placeholder for the Site Supervisor's home screen.
// You can create a new component for the site supervisor's dashboard and import it here.
import DefaultHomePage from './default-home';

export default function SiteSupervisorHome() {
    return <DefaultHomePage />;
}
